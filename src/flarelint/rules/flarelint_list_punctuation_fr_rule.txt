rule: error

extensions: htm html flsnp

when: self li and next li and parent ul and language fr

test: self-ends li ";"

message:

Incorrect punctuation in a bullet list (`ul`). Each item, except the
last, in a bullet list must be an uncapitalized phrase (except, of
course, product and proper names) and end with ";". To fix, adjust the
punctuation.
